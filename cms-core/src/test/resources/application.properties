# Shared test configuration for SQLite-based unit tests
# Uses SQLite in-memory database for lightweight, fast test execution
quarkus.datasource.db-kind=sqlite
quarkus.datasource.jdbc.url=jdbc:sqlite:mem:test
quarkus.datasource.jdbc.driver=org.sqlite.JDBC

# Custom Hibernate dialect for SQLite support (hibernate-community-dialects)
quarkus.hibernate-orm.dialect=org.hibernate.community.dialect.SQLiteDialect

# Drop and create schema for each test run
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.log.sql=false

# Disable JSON format mapper checks (SQLite does not have native JSONB type)
quarkus.hibernate-orm.mapping.format.global=ignore

# Disable Flyway for tests (we use drop-and-create)
quarkus.flyway.migrate-at-start=false

# Disable DevServices
quarkus.devservices.enabled=false

# CMS schema init
cms.schema.init.enabled=false

# Logging
quarkus.log.level=WARN
