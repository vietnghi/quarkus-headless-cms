/**
 * Custom fields module for extending content entries with arbitrary metadata.
 *
 * <p>Defines custom field metadata per content type (name, type, default, required) and provides a
 * service for reading/writing those values as a separate JSON payload that integrates with the
 * existing data storage.
 */
package com.quarkus.cms.customfields;
