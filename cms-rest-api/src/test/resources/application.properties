# Test configuration for cms-rest-api module
quarkus.cms.enabled=true

# Use SQLite database for tests (PostgreSQL mode for JSONB support)
quarkus.datasource.db-kind=sqlite
quarkus.datasource.jdbc.url=jdbc:sqlite:/tmp/test-rest_api.db?cache=shared
quarkus.datasource.jdbc.driver=org.sqlite.JDBC
quarkus.hibernate-orm.dialect=org.hibernate.community.dialect.SQLiteDialect

quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.log.sql=false
quarkus.hibernate-orm.mapping.format.global=ignore

quarkus.log.level=WARN
quarkus.http.test-port=0

# OpenAPI / Swagger UI (for documentation tests)
quarkus.smallrye-openapi.path=/openapi
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger
mp.openapi.extensions.smallrye.info.title=Quarkus Headless CMS API (Test)
mp.openapi.extensions.smallrye.info.version=1.0.0-SNAPSHOT

# Disable Quarkus DevServices — tests use explicit Testcontainers
quarkus.devservices.enabled=false
