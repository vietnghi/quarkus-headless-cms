# Test configuration for cms-draft-publish module
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
quarkus.datasource.jdbc.max-size=2

# Drop and create the database on startup
quarkus.hibernate-orm.database.generation=drop-and-create

# Disable schema initialization (core_schema table uses JSONB, unsupported by H2)
cms.schema.init.enabled=false

# Log SQL for debugging during tests
quarkus.hibernate-orm.log.sql=false
